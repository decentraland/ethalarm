class AlarmsService {

  constructor(db) {
  }

  getAlarms() {
    // Just query the database
  }

  storeNewAlarm(alarmDescription) {
    // store into the database
  }

  getHooksForAlarm(alarm) {
    // query the database
  }
}
